import { PressableProps } from "react-native";

type Props = PressableProps & {
  children: React.ReactNode;
};

export default function AppPressable({ children }: Props) {
  return <> </>;
}
